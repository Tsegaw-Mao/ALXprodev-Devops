#!/bin/bash

# URL for Pikachu data
URL="https://pokeapi.co/api/v2/pokemon/pikachu"

# Output files
DATA_FILE="data.json"
ERROR_FILE="errors.txt"

# Fetch data
if ! curl -s -f "$URL" -o "$DATA_FILE"; then
    echo "Failed to fetch data from $URL" >> "$ERROR_FILE"
fi
